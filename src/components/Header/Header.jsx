import SearchBar from './SearchBar';
import EmojiPage from './EmojiPage';

const Header = () => {
  return (
    <header>
      <SearchBar />
      <EmojiPage />
    </header>
  );
};

export default Header;
