import Navigation from '../Navigation';
import Logo from '../Logo';

const Sidebar = () => {
  return (
    <header>
      <Logo />
      <Navigation />
    </header>
  );
};

export default Sidebar;
